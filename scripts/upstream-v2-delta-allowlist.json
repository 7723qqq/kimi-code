{
  "note": "Verdicts for upstream commits touching packages this fork deleted. Regenerate with `bun scripts/check-upstream-v2-delta.mjs --update`; see scripts/check-upstream-v2-delta.mjs for the gate itself. `tracked` = accepted debt recorded in packages/kimi-agent/ROADMAP.md §6; `not-applicable` = internal refactor with no observable engine behavior to port.",
  "upstreamRef": "upstream/main",
  "recordedMergeBase": "1b89e4b039f052d10f258464413b2047acca12ba",
  "recordedAt": "2026-09-17",
  "entries": [
    {
      "sha": "1fddc16e3ea2de4c26a18acd764380adf9e2ed64",
      "date": "2026-09-18",
      "subject": "fix(agent-core-v2): drop the outside-working-directory access ban from the system prompt (#3879)",
      "verdict": "ported",
      "note": "The engine's own system-prompt template carried the same sentence the commit removes from v2's system.md: packages/kimi-agent/src/prompt/system.md:108 ended `Unless the user explicitly instructs otherwise, never read, write, or execute files outside the working directory.` That sentence is dropped, leaving the 'not a sandbox' framing upstream keeps. Behavior, not just a doc string: the sentence is templated into every turn's system prompt."
    },
    {
      "sha": "c5ad17f06aa12702d2b30dff63acba9ed08f688d",
      "date": "2026-09-17",
      "subject": "feat(agent-core-v2): support api_key_env for provider credentials (#3762)",
      "verdict": "tracked",
      "note": "api_key_env for provider credentials. The fork's provider credential path runs through packages/oauth (managed-kimi-code / refreshProviderModels), which the merge keeps in sync with upstream, but this commit reshapes provider credential resolution across oauth and the retired engine (80 files). Needs its own triage round against the fork's credential flow before porting; recorded as debt with the upstream commit as the reference."
    },
    {
      "sha": "86e08208e3445171f6500c948234d7e3a0684324",
      "date": "2026-09-17",
      "subject": "feat(agent-core-v2): approve unanalyzable bash commands in yolo permission mode (#3869)",
      "verdict": "not-applicable",
      "note": "No counterpart state exists in this engine, so there is nothing to approve or ask about. v2's DangerousCommandAsk policy has three outcomes (dangerous / unanalyzable / no-verdict) because it feeds each Bash command through the tree-sitter bash parser, which can reject input: an unparseable command became a distinct `unanalyzable` verdict and was asked about in every mode, which is what #3869 relaxes for yolo. The fork's analyzer is `native/permission_engine/dangerous_command.rs::analyze_bash_command`, a hand-written tokenizer whose signature (`fn(&str) -> DangerousVerdict`, two variants Dangerous/Safe) cannot express a parse failure — `tokenize_command` returns `Vec<String>` with no error path — so an unrecognizable command can only fall through as Safe to the normal chain. Verified empirically rather than by reading: a temporary unit test ran ten inputs through the real function and every unparseable one (`echo 'unclosed`, `if [ -f x ]; then`, `for i in 1 2 3; do`, `$((`, `;;;`, `)))`, `` `\\` ``, the empty string, and blanks) returned Safe while `sudo reboot` returned Dangerous(\"reboot\"); the probe was then removed. Consequence: in yolo mode our engine already approves these commands (YoloModeApprove, `permission/mod.rs:440`), and in ask mode they already reach the user's normal approval decision rather than a special unanalyzable prompt — the behavior #3869 asks for, reached by a different route. No port needed."
    }
  ]
}
