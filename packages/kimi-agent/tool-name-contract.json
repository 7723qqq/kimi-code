{
  "v2Native": [
    "Read",
    "Write",
    "Edit",
    "Bash",
    "Grep",
    "Glob",
    "FetchURL",
    "WebSearch",
    "AskUserQuestion",
    "TodoList",
    "EnterPlanMode",
    "ExitPlanMode",
    "CreateGoal",
    "GetGoal",
    "UpdateGoal",
    "SetGoalBudget",
    "CronList",
    "CronCreate",
    "CronDelete",
    "TaskList",
    "TaskOutput",
    "TaskStop",
    "Skill",
    "Agent"
  ],
  "v2Host": [
    "WaitFor",
    "AgentSwarm",
    "select_tools",
    "TowerInit",
    "TowerPlan",
    "TowerSpawn",
    "TowerMerge",
    "TowerTeardown",
    "TowerSend",
    "TowerInbox",
    "TowerFinding",
    "TowerReview",
    "TowerMission",
    "TowerStatus"
  ],
  "replOnlyNative": [
    "invoke_subagent",
    "manage_subagents",
    "define_subagent",
    "TaskWait",
    "list_directory",
    "Knowledge",
    "Team"
  ],
  "unloadedInV2": [],
  "v2Github": [
    "GitHubGetRepo",
    "GitHubListBranches",
    "GitHubListCommits",
    "GitHubGetCommit",
    "GitHubGetFileContents",
    "GitHubCreateOrUpdateFile",
    "GitHubGetRef",
    "GitHubCreateTree",
    "GitHubCreateCommit",
    "GitHubUpdateRef",
    "GitHubCreateBranch",
    "GitHubListIssues",
    "GitHubGetIssue",
    "GitHubCreateIssue",
    "GitHubUpdateIssue",
    "GitHubAddIssueComment",
    "GitHubListIssueComments",
    "GitHubListPRs",
    "GitHubGetPR",
    "GitHubGetPRDiff",
    "GitHubGetPRFiles",
    "GitHubCreatePR",
    "GitHubUpdatePR",
    "GitHubMergePR",
    "GitHubCreatePRReview",
    "GitHubListPRReviewComments",
    "GitHubSearchCode",
    "GitHubSearchRepos",
    "GitHubSearchIssues",
    "GitHubListWorkflowRuns",
    "GitHubGetWorkflowRun",
    "GitHubListReleases",
    "GitHubGetLatestRelease",
    "GitHubGetMe"
  ],
  "aliases": {
    "fetch_url": "snake twin of FetchURL — subagent/manager.rs allowlists and permission/mod.rs match it",
    "web_search": "snake twin of WebSearch — subagent/manager.rs allowlists and permission/mod.rs match it",
    "listdirectory": "collapsed twin of list_directory (no def advertises either spelling)",
    "invokesubagent": "collapsed twin of invoke_subagent (the REPL def name, subagent_tools.rs)",
    "managesubagents": "collapsed twin of manage_subagents (the REPL def name, subagent_tools.rs)",
    "definesubagent": "collapsed twin of define_subagent (the REPL def name, subagent_tools.rs)",
    "ask_user_question": "snake twin of AskUserQuestion — this snake form IS the REPL def name (ask_user_question.rs)",
    "get_goal": "snake twin of GetGoal — permission/mod.rs matches it",
    "todo_list": "snake twin of TodoList — permission/mod.rs matches it",
    "enter_plan_mode": "snake twin of EnterPlanMode — permission/mod.rs matches it",
    "exit_plan_mode": "snake twin of ExitPlanMode — permission/mod.rs matches it",
    "create_goal": "snake twin of CreateGoal — permission/mod.rs matches it",
    "update_goal": "snake twin of UpdateGoal — permission/mod.rs matches it",
    "set_goal_budget": "snake twin of SetGoalBudget — permission/mod.rs matches it",
    "cron_list": "snake twin of CronList — permission/mod.rs matches it",
    "cron_create": "snake twin of CronCreate — permission/mod.rs matches it",
    "cron_delete": "snake twin of CronDelete — permission/mod.rs matches it",
    "task_list": "snake twin of TaskList — permission/mod.rs matches it",
    "task_output": "snake twin of TaskOutput — permission/mod.rs matches it",
    "task_stop": "snake twin of TaskStop — repl/mod.rs slash mapping matches it",
    "task_wait": "snake twin of the replOnly TaskWait — permission/mod.rs matches it"
  },
  "notes": {
    "WaitFor": "v2's task-wait tool registers as 'WaitFor' (taskWaitTool.ts:326); the engine's native twin is the REPL's 'TaskWait'. The v2 name lowercases to 'waitfor', which handles() has never accepted — WaitFor round-trips to the host. P34's claim 'WaitFor→taskwait 能命中' was wrong; corrected here.",
    "TaskWait": "REPL-only def (task_tools.rs:611). No v2 product tool is named TaskWait — v2's task-wait tool is WaitFor (host-owned).",
    "list_directory": "Executable natively (tools/mod.rs execute()) but advertised by NO tool def anywhere — not even build_repl_tool_defs. G-8's 'REPL 可达' was wrong; corrected here.",
    "Agent": "P46 (2026-09-02): the foreground core of v2's Agent tool (agentTool.ts) executes natively — the host pushes the session's profile catalog per turn and the engine runs the subagent through the same native pipeline. Routing is conditional: resume / fork / background / model args, profiles missing from the snapshot (plugin sources, external backends) and runtime-less transports fall back to the host tool, which stays registered. Pre-P46 this entry read 'host-owned, invoke_subagent family REPL-only' (G-8).",
    "ReadMediaFile": "Not a registered tool anymore — Read handles media internally (readTool.ts:308 via execute-media-read). The name survives only in permission lists (matchesRule.ts:82, default-tool-approve.ts:9) and TUI renderers. G-8 listed it as a v2 registered name; corrected here.",
    "Knowledge": "The v2 tool module (agent/knowledge/tools/knowledge-tool.ts) self-registers but NOTHING imports it — dead v2-side registration. The REPL advertises its own native twin (repl/mod.rs knowledge_tool_def). G-8/batch4 treated Knowledge as a live v2 tool; corrected here.",
    "Team": "Same as Knowledge: teamTool.ts self-registers but is imported by nothing; the REPL has a native twin (repl/mod.rs team_tool_def). The fork advertises Team as a feature — the v2-side wiring is gone.",
    "run_code": "Fully unreachable: codeRuntimeFeature is imported by no module (not even index.ts), there is no native arm and no REPL def. batch4 assessment treated it as a live v2 tool to evaluate; corrected here.",
    "lsp": "Fully unreachable: lspFeature is imported by no feature assembly (only its errors module feeds errors.ts), no native arm, no REPL def.",
    "session_query": "Fully unreachable: sessionQueryFeature is imported by no feature assembly (only its errors module feeds errors.ts), no native arm, no REPL def.",
    "unloadedInV2": "M3a (2026-09-01) deleted the unloaded tool features (lsp / sessionQuery / codeRuntime / knowledge / team / workflow / memory / attachment) from agent-core-v2 entirely — the section is kept empty so a re-wired tool fails the TS unclassified check instead of silently passing.",
    "mcp": "MCP runtime-discovered tools are host-owned by design: handles() never matches them, so they round-trip to the host's MCP runtime. The engine's own McpManager serves them only in REPL standalone mode (execute_tool's fallback arm).",
    "github": "GitHub tool names are a dynamic family matched by github::is_github_tool against its SPECS table, not part of NATIVE_TOOL_NAMES. v2Github pins the v2 registration set to the engine's SPECS — a tool added on one side fails the other side's test.",
    "aliases": "The snake spellings are load-bearing: subagent/manager.rs allowlists, permission/mod.rs and repl/mod.rs match them. Do not prune without sweeping those call sites.",
    "scope": "The v2 side of this contract is enumerated from the real tool registry (features assembled with every experimental flag on, then IAgentToolRegistryService.list()) — not a hand-maintained list. Tools registered by the app layer outside agent-core-v2 are host-owned by construction: handles() rejects any name not in NATIVE_TOOL_NAMES."
  }
}
