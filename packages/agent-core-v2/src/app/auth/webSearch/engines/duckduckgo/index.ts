/**
 * `auth` domain (cross-cutting) — DuckDuckGo search engine entry.
 *
 * Ported from the open-websearch project
 * (`package/build/engines/duckduckgo/index.js`), request mode only.
 */

export { searchDuckDuckGo } from './searchDuckDuckGo';
