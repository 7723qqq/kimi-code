/**
 * `auth` domain (cross-cutting) — Startpage search engine entry, ported from
 * the open-websearch project (`engines/startpage`). Request-mode scraping
 * only; the original has no playwright path.
 */

export { searchStartpage } from './startpage';
