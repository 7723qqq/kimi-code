/**
 * `auth` domain (cross-cutting) — GitHub README fetcher entry, ported from
 * the open-websearch project (`engines/github`). Request mode only.
 */

export { fetchGithubReadme } from './github';
