/**
 * `auth` domain (cross-cutting) — CSDN search engine and article fetcher
 * entry, ported from the open-websearch project (`engines/csdn`). Request
 * mode only.
 */

export { searchCsdn } from './csdn';
export { fetchCsdnArticle } from './fetchCsdnArticle';
