/**
 * `kosong/contract` domain — the ChatProvider wire contract.
 *
 * Re-export layer: the implementation moved to `@moonshot-ai/kosong/provider`
 * (shared contract layer). Keep this file as a thin re-export so existing
 * `#/kosong/contract/provider` imports stay valid.
 */

export * from '@moonshot-ai/kosong/provider';
