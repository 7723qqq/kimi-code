/**
 * `kosong/contract` domain — declared model capabilities.
 *
 * Re-export layer: the implementation moved to `@moonshot-ai/kosong/capability`
 * (shared contract layer). Keep this file as a thin re-export so existing
 * `#/kosong/contract/capability` imports stay valid.
 */

export * from '@moonshot-ai/kosong/capability';
