/**
 * `kosong/contract` domain — the provider error taxonomy.
 *
 * Re-export layer: the implementation moved to `@moonshot-ai/kosong/errors`
 * (shared contract layer). Keep this file as a thin re-export so existing
 * `#/kosong/contract/errors` imports stay valid.
 */

export * from '@moonshot-ai/kosong/errors';
