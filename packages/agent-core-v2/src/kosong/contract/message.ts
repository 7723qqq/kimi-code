/**
 * `kosong/contract` domain — wire message shapes and their pure helpers.
 *
 * Re-export layer: the implementation moved to `@moonshot-ai/kosong/message`
 * (shared contract layer). Keep this file as a thin re-export so existing
 * `#/kosong/contract/message` imports stay valid.
 */

export * from '@moonshot-ai/kosong/message';
