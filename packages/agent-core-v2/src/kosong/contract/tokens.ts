/**
 * `kosong/contract` domain — character-based token-count estimates for
 * messages, tools, and content parts.
 *
 * Re-export layer: the implementation moved to `@moonshot-ai/kosong/tokens`
 * (shared contract layer). Keep this file as a thin re-export so existing
 * `#/kosong/contract/tokens` imports stay valid.
 */

export * from '@moonshot-ai/kosong/tokens';
