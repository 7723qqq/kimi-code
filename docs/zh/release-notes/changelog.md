---
outline: 2
---

# 变更记录

本页记录 Kimi Code CLI 每个版本的变更内容。

## 0.26.0（2026-07-16）

### 新功能

- 扩展 coder 子 Agent 的工具集，新增后台任务、待办列表、Plan 模式、Skill 调用和嵌套 Agent 支持，与主 Agent 能力对齐；子 Agent 运行完成后会等待后台任务全部结束再报告完成。

### 优化

- 优化上下文用量显示的单位格式。
- 在 /model 和 /effort 选择器中提示切换模型会失效现有提示缓存，建议使用 /new 开启新会话以避免额外的 token 消耗。
- web: 打开模型选择器时会刷新所有提供商的模型目录，确保新模型立即可见。

### 修复

- 修复通过 Anthropic 协议路由的 Kimi 模型错误显示推理强度选项的问题。
- 修复 OpenAI 兼容提供商上显式关闭思考功能无效的问题：此前关闭状态与未配置无法区分，导致历史自动推理强度注入仍会开启模型思考。
- web: 修复侧边栏调整手柄被聊天输入框背景遮挡的问题。
- 修复跨多个 Kimi 主目录时旧版迁移可能重复执行的问题，并妥善报告损坏或映射丢失的会话。
- 修复 Anthropic 兼容和 Kimi 保留思考端点上回放空思考内容时替代为占位空格的问题，现保持原样回放。
- 修复用户停止任务后未在模型上下文中记录停止原因的问题。
- 修复恢复会话时被错误标记为刚刚更新而跳转到会话列表顶部的问题。
- 修复手动停止后台子 Agent 后立即恢复可能报"已在运行"错误的竞态问题。
- 修复上下文大小指示器低估模型实际上下文使用量的问题。

## 0.25.0（2026-07-16）

### 新功能

- web: 聊天支持附加任意类型文件，可直接将文件拖放到窗口任意位置；发送的文件、图片、视频都会以附件标签显示在消息气泡中。

### 优化

- web: 模型请求失败时展示完整诊断信息。
- 应用 Anthropic 官方 effort 配置，未知模型回退到 128k 输出上限。

### 修复

- 修复 Web 服务器 bearer token 校验可被百分号编码的 API 路径绕过、导致所有 API 路由可被未认证访问的问题。
- 修复会话文件系统 API 可跟随指向工作区外的符号链接、导致宿主机文件被越权访问的问题。
- web: 会话活动指示器现与 Agent 实际工作保持同步；修复会话激活竞争或 LLM 重试后流式内容重复的问题。
- 修复 Anthropic 兼容供应商中自定义命名模型新会话思考强度被错误关闭、且 ACP 客户端不显示思考强度控件的问题。
- Anthropic 兼容模型现在正确遵循 adaptive_thinking = false，请求中不再携带 effort 参数。
- web: 修复服务器绑定非回环地址时 CSP 阻止 Web UI 主题初始化脚本与内置字体加载的问题。
- 修复工作区目录通过符号链接给出时会话创建失败的问题。
- 修复剪贴板图片读取失败导致 CLI 意外退出的问题，现在会回退为粘贴文本。
- web: 修复已完成的后台子 Agent 在会话重新加载后丢失最终输出的问题。
- web: 修复开发构建中 Enter 键无法确认模态对话框的问题。
- web: 修复流式输出期间后台子 Agent 在 agents dock 面板中显示为两行相同记录的问题。
- 修复 CLI 意外退出时诊断日志缺少实际错误信息的问题。