---
"@moonshot-ai/kimi-code": patch
---

Fix the experimental GitHub tools not appearing in sessions even when enabled. Enable them with `[experimental] github_tools = true` or `KIMI_CODE_EXPERIMENTAL_GITHUB_TOOLS=1` and a `GITHUB_TOKEN`.
