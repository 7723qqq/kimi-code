---
'@moonshotai/kimi-code': patch
---

Wire the configuration surface the file schema already promised but the engine never read: `[providers.*].customHeaders` now reaches the HTTP transport (self-hosted gateways no longer 401), `[models.<alias>] .baseUrl` / `.protocol` / `.betaApi` override the provider defaults — an anthropic-protocol alias declaring `betaApi` really posts to `/messages?beta=true` instead of the standard endpoint — `[tools]` builds the policy filter on the file-reading entries, `[thinking].effort` (plus a model's `offEffort` when thinking is off) goes on the wire, `[permission].rules[].reason` is echoed in the denial, `default_permission_mode` is the fallback mode, `[experimental]` flags are reported by `/api/v1/meta` instead of a constant, and `extra_skill_dirs` feed both the skill listing and the system prompt. `[background]` now reaches the engine on **every** entry point, not just the REPL: the task runner's stop grace and concurrent-task cap (`max_running_tasks` also honors `KIMI_CODE_BACKGROUND_MAX_RUNNING_TASKS`), whether a timed-out Bash call migrates to the background, and the background Bash task timeout (`bash_task_timeout_s`, `0` = none), so `--serve` / ACP / engine sessions build a runner the same way the REPL does. Entry points agree: the REPL resolves `[loop_control]`, `[thinking].keep` and `[secondary_model]`; `--serve` connects `[mcp_servers]`; ACP loads `[permission]` and `[hooks]`.
