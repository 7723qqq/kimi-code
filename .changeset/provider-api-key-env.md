---
"@moonshot-ai/kimi-code": patch
---

Support `[providers.*] api_key_env` so a provider credential can be read from an environment variable at request time instead of being stored in the config file.
