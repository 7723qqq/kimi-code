---
"@moonshot-ai/kimi-code": patch
---

TUI footer now shows per-session stats once a session has traffic: turns, steps, LLM/tool time, average first-token latency, and input/output token counts. The cache display switches to "cache hit %" wording.
