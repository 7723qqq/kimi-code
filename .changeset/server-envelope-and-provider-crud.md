---
'@moonshot-ai/kimi-code': patch
---

Serve all local-server JSON responses in the envelope the Web UI expects, and add provider create/replace/delete/read so the model settings page can manage providers in config.toml.
