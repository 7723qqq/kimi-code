---
"@moonshot-ai/kimi-code": minor
---

GitHub tools graduated from an experimental feature to a standard one, and now appear only when a GitHub token is available. Set `token` under `[github]` in `config.toml`, or use the `GITHUB_TOKEN` / `GH_TOKEN` environment variable; set `base_url` to reach a GitHub Enterprise Server instance.
