---
"@moonshot-ai/kimi-code": patch
---

Report MCP server status transitions to the UI: a server that finishes connecting (or fails) after startup now leaves the "connecting…" state instead of spinning forever.
