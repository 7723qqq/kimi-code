---
"@moonshot-ai/kimi-code": patch
---

Add engine-side `/btw` side-channel turns (`sessionStartBtw` / `sessionBtwPrompt` / `sessionBtwCancel`) so the btw panel runs tool-free turns forked from the live session history, with SDK prompt/cancel routing.
