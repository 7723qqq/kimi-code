---
"@moonshot-ai/kimi-code": minor
---

Add experimental GitHub Git Data tools — get refs, create trees, commits, and branches, and update refs — so commits can be pushed through the API without local git credentials. Enable with KIMI_CODE_EXPERIMENTAL_GITHUB_TOOLS=1.
