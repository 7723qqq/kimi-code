---
"@moonshot-ai/kimi-code": minor
---

Enable the built-in GitHub tools by default in new sessions. Set a GITHUB_TOKEN (or GH_TOKEN) environment variable or a token in the [github] config section to use them; disable with `[experimental] github_tools = false`.
