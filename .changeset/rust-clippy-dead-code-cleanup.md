---
"@moonshot-ai/kimi-code": patch
---

Kimi-native-tools: fix all clippy errors and clear the dead-code backlog. Module-header docs after an outer doc comment (`escape`, `image_compress`, `output_truncate`, `translation`, `tool_naming`) are now inner `//!` docs; redundant closures (`knowledge`, `napi_bindings`), an explicit `.into_iter()` in `zip`, match-to-`?` rewrites (`translation`, `goal/accounting`), a needless borrow, a `format!` no-op, and a manual clamp (`workspace_index`) are rewritten per clippy — `cargo clippy --lib --tests` is now warning- and error-free. Dead code removed: the never-exported `GoalAccountingState` stateful turn/wall-clock machinery (the TS runtime owns accounting baselines; the stateless `TokenUsage` + `goal_token_delta` core is kept, plus a new negative-delta regression test), the duplicate `apply_and_report` (identical to `engine::apply_usage`), zero-use `GoalStatus::is_active/is_resumable/is_terminal`, the never-read `LlmStreamConfig.model` (the napi config field stays for wire compatibility and is documented as accepted-but-unused), and `WorkspaceIndex`'s `mtime` capture (saves one `modified()` syscall per indexed file), `root`, and `paths()`. Test-only helpers (`GoalState::new`, `remaining_tokens`) are marked `cfg_attr(not(test), allow(dead_code))` with rationale, and `WorkspaceIndex::get` is now exercised via `predict_read`.
