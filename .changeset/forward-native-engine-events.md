---
"@moonshot-ai/kimi-code": patch
---

Forward native-engine step, tool-call-delta and subagent lifecycle events to the UI, so the step counter, streaming tool arguments and subagent cards update during a turn.
