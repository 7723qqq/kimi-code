---
"@moonshot-ai/kimi-code": patch
---

Remember an extra workspace directory for the whole project: choose "remember" in `/add-dir` and it is stored in the project's local configuration and used by every later session of that checkout.
