---
"@moonshot-ai/kimi-code": patch
---

Serve the official Web UI's v2 session list shape and batch archive/restore from the native server, so `kimi web` works against the bundled assets without a separate backend.
