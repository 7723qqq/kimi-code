---
"@moonshot-ai/kimi-code": patch
---

Run the built-in GitHub tools natively in the Rust engine, honor the `[github]` config section (token, base URL) there, and auto-approve read-only GitHub calls in manual permission mode.