---
"@moonshot-ai/kimi-code": patch
---

A mistyped MCP transport no longer registers a server that answers with fabricated results, remote MCP URLs must be http(s), and a server's stored credentials are no longer sent to a message endpoint on another host.
