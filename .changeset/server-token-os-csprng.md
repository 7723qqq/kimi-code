---
'@moonshot-ai/kimi-code': patch
---

The local server's API token is now minted from the operating system's cryptographic random source, so a generated token can no longer be predicted from a seeded pseudo-random generator.
