---
"@moonshot-ai/kimi-code": patch
---

Surface the engine events the UI was missing: background-task lifecycle, automatic compaction, external-hook results, goal updates, live `!` shell command output, and cron jobs now fire on schedule instead of never running.
