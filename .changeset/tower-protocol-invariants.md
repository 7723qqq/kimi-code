---
"@moonshot-ai/kimi-code": patch
---

Tower missions now refuse to complete with unfinished tasks or an empty diff, review rounds stop after five attempts, and `AgentSwarm` is rejected while a tower is active instead of pausing it.
