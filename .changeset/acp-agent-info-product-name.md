---
'@moonshot-ai/kimi-code': patch
---

Report the ACP agent's product name (`Kimi Code CLI`) in the `initialize` response instead of the internal engine name, so IDEs that display `agentInfo.name` show the product rather than `kimi-agent-rust`.
