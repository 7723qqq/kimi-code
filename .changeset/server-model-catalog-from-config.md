---
'@moonshot-ai/kimi-code': patch
---

Serve the configured models and providers (with credential state) from the Web API instead of a built-in sample list, and add GET /api/v1/auth for sign-in readiness.
