---
'@moonshot-ai/kimi-code': patch
---

Register `tower` and `xunfei_coding_plan` in the host's experimental-flag registry, so the `/tower` gate and the Astron settings row resolve again. The desktop command's description is now localized, and `/team` and `/workflow` report a missing session instead of telling a signed-in user to log in.
