---
"@moonshot-ai/kimi-code": patch
---

Stop asking for approval on read-only tools (memory reads, LSP queries, tower readers) in "Always Ask" mode; they now run without a prompt like the other read-only tools.
