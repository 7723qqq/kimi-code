/**
 * TUI2 compaction — forwarding layer.
 *
 * Status: REAL (tui2). Forwards to `compaction.tsx`.
 */
export * from './compaction.tsx';
export { CompactionView as CompactionComponent } from './compaction.tsx';
