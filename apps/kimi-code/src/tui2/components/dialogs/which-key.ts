/**
 * TUI2 which-key — forwarding layer.
 *
 * Status: REAL (tui2). Forwards to `which-key.tsx`.
 */
export * from './which-key.tsx';
export { WhichKey as WhichKeyComponent } from './which-key.tsx';
export type { WhichKeyProps as WhichKeyOptions } from './which-key.tsx';
