/**
 * TUI2 agent activity viewer — forwarding layer.
 *
 * Status: REAL (tui2, body uses v1 message renderer). Forwards to
 * `agent-activity-viewer.tsx`.
 */
export * from './agent-activity-viewer.tsx'