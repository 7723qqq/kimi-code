{
  "name": "@moonshot-ai/vis",
  "version": "0.1.1",
  "private": true,
  "description": "Debug visualization tool for kimi-code sessions",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:deps": "(cd web && bun run build) && (cd server && bun run build)",
    "predev": "bun run build:deps",
    "dev": "bun scripts/dev.mjs",
    "build": "bun run build:deps && node scripts/copy-web-dist.mjs",
    "prestart": "bun run build",
    "start": "bun server/dist/server.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
